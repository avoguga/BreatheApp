export type Averages = {
  dailyAverage: number;
  weeklyAverage: number;
  monthlyAverage: number;
};

export type WorkedTime = {
  daysWorked: number;
  weeksWorked: number;
  monthsWorked: number;
};
