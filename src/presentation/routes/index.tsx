import { NavigationContainer } from "@react-navigation/native";
import { StackNavigation } from "./stack";

export const Routes = () => (
  <NavigationContainer>
    <StackNavigation />
  </NavigationContainer>
);
